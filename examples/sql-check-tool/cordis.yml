# sql-check-tool composition: the tool registry, the system-prompt assembler
# it wires schemas into, and the demo plugin. A Loader boots this file directly
# as a config tree; the test suite mounts the same three services in-process.
- id: system-prompt
  name: '@deepseek-ai/dsh-system-prompt'

- id: tools
  name: '@deepseek-ai/dsh-tools'

- id: sql-check-tool
  name: './src/index.ts'
